import { StyleSheet } from 'react-native';

/**
 * 스타일 지정
 */
const GlobalStyles = StyleSheet.create({
    titleText: {
        fontSize: 25,
        color: 'white'
    },
});

export default GlobalStyles;